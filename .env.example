# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/trove_db

# Auth - generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# Environment
ENVIRONMENT=development

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend URL (for OAuth redirect after callback)
FRONTEND_URL=http://localhost:5173

# CORS — comma-separated allowed origins (required in production)
CORS_ORIGINS=

# Cloud Storage — point local dev at the dev bucket, never the production one.
#
# No keys here: authentication is application default credentials, so run
#   gcloud auth application-default login
# once and the bucket is reachable as you. The public URL is derived from this
# name, so there is nothing else to set.
STORAGE_BUCKET_NAME=trove-images-dev

# Sentry — leave empty for local development.
#
# An empty DSN disables Sentry entirely: no SDK init, nothing sent. Verifying
# the integration locally means temporarily pasting the real DSN here, which is
# deliberately an act rather than a default, so test exceptions do not land in
# the production issue stream by accident.
#
# SENTRY_DSN=

# Email (Resend) — leave empty for local development.
#
# With no key nothing is sent and the message, reset link included, is logged to
# stdout instead, so the forgot-password flow can be walked end to end locally
# with no Resend account and no real inbox to receive at.
#
# That substitution is gated on ENVIRONMENT=development. Anywhere else an empty
# key logs an error and no link, because the link is a working password reset
# token for a named account and a log file is the wrong place to keep one.
#
# RESEND_API_KEY=

# Sender address. Only mail.trovebox.io is authorised to send — its DKIM and
# return-path records live in trove-infra — so overriding this is for testing
# against your own verified Resend domain, not for production.
# EMAIL_FROM=Trove <noreply@mail.trovebox.io>
